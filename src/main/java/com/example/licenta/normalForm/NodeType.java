package com.example.licenta.normalForm;

public enum NodeType {
    VAR,       // variabilă: A, B, C
    NOT,       // ¬A
    AND,       // A ∧ B
    OR,        // A ∨ B
    IMPLIES,   // A → B
    IFF        // A ↔ B
}
